services:
  terraform:
    image: hashicorp/terraform:1.14.5
    working_dir: /work
    volumes:
      - ./:/work
    environment:
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - AWS_SESSION_TOKEN
      - AWS_DEFAULT_REGION=eu-central-1